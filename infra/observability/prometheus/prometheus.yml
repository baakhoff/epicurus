global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # epicurus services expose Prometheus metrics at /metrics via epicurus-core.
  - job_name: echo
    static_configs:
      - targets: ["echo:8080"]
